import { Typography } from '@mui/material';

const ErrorMessage = () => {
  return <Typography variant='h5'>Что-то пошло не так, не получилось загрузить данные о погоде</Typography>
}

export default ErrorMessage;
